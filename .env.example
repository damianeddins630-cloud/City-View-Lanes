AUTH_SECRET=replace-with-a-long-random-string
SITE_URL=http://localhost:3000

# Required on Vercel so profile/hours/leagues/bookings actually persist
# Create at: Vercel Dashboard → Storage → Blob → Connect → copy token
BLOB_READ_WRITE_TOKEN=
