CityView Lanes — make saves stick on Vercel
==========================================

Your Profile / Hours will show an error until Blob is connected.
That error means the live site is still in "memory" mode.

IMPORTANT: Always upload this zip into your EXISTING Vercel project.
Do NOT click "Create New Project" every time, or Blob settings get lost.

Step-by-step (about 2 minutes)
------------------------------
1. Download the newest zip from GitHub Releases (cityview-v3 or newer).

2. Open https://vercel.com → open the project that runs your LIVE CityView URL.

3. Upload / deploy that zip into that SAME project.

4. Click Storage → Create Database / Blob
   - Name it anything (e.g. cityview-data)
   - Private or Public both work with this build
   - Connect it to THIS project (Production)

5. Confirm env vars exist:
   Settings → Environment Variables
   You should see at least one of:
   - BLOB_STORE_ID
   - BLOB_READ_WRITE_TOKEN

6. Deployments → open the latest deployment → Redeploy
   (Required after connecting Blob.)

7. Open your live site → Login → Admin
   - Banner should say Saves are on / mode blob (not memory)
   - Click "Test save now" → must say PASS

8. Edit Profile + Hours again — they should stay after refresh.

If mode is still "memory"
-------------------------
You are looking at a different Vercel project than the one with Blob,
or you have not Redeployed after connecting Blob.

Master admin
------------
Username: cityview_damian
Password: Archer6!9_20
